# 일어나시면 여기부터 — 관리자 표준화 진행 상황

작성 2026-09-02 · 브랜치 `admin-standard` · **아직 배포 안 함 (라이브 사이트 그대로)**

---

## 한 줄 요약

**코드는 다 만들어 뒀습니다.** 이제 원장님이 계정 2개(Supabase·카카오)만
만들어 주시면, 제가 연결하고 배포까지 끝냅니다.

---

## 지금 어디까지 됐나

| 단계 | 내용 | 상태 |
|---|---|---|
| 1 | DB 표 만들 SQL 5개 | ✅ 준비됨 (`supabase/01~05`) |
| 2 | 관리자 권한 잠그기 SQL | ✅ 준비됨 (`supabase/05`) |
| 3 | 관리자 껍데기 (메뉴·레이아웃·로그인) | ✅ 완료 |
| 4 | 병원 정보 모듈 | ✅ 완료 |
| 5 | 칼럼 관리 (**새 글 쓰기 추가**) | ✅ 완료 |
| 6 | 팝업 관리 | ✅ 완료 |
| 7 | 후기 관리 + 공개 후기 페이지 | ✅ 완료 |
| 8 | 카카오 로그인 + 회원 관리 | ✅ 완료 |
| 9 | 기존 3개 기능 이식 | ✅ 완료 |
| 10 | **연결 + 배포** | ⬜ **원장님 차례** |

빌드·타입검사 모두 통과했습니다. 다만 **Supabase가 없어서 실제 동작은
아직 확인 못 했습니다.** 화면 모양까지만 검증된 상태입니다.

---

## 관리자 메뉴가 이렇게 바뀝니다

**전** — 탭 4개: 칼럼 관리 / 발행 큐 / 색인 현황 / 사이트 점수

**후** — 표준 6개 메뉴 (다른 병원 사이트와 동일)

```
홈 · 병원 정보 · 팝업 관리 · 칼럼 관리 · 후기 관리 · 회원 관리
```

기존 3개 기능은 버리지 않고 옮겨 담았습니다.
- 발행 큐, 색인 현황 → **칼럼 관리** 안의 탭
- 사이트 점수 → **홈**

**새로 생긴 것**
- 병원 정보(전화·주소·진료시간 등)를 화면에서 수정 — 지금까진 코드를 고쳐야 했습니다
- **칼럼 새로 쓰기** — 지금까진 삭제만 됐습니다
- 팝업·후기 등록
- 회원 목록과 동의 기록
- AI(챗GPT·클로드 등)가 우리 글을 읽어 갔는지 기록

---

## 원장님이 해주실 것 (3가지)

### ① Supabase 프로젝트 만들기 (5분)

1. https://supabase.com 접속 → 로그인 → **New project**
2. 이름 아무거나(예: `mail100hanwon`), 지역은 **Northeast Asia (Seoul)**
3. 데이터베이스 비밀번호는 아무거나 정하고 **어딘가 적어두세요**
4. 만들어지면 왼쪽 **Settings → API** 로 가서 아래 2개를 복사해 주세요
   - **Project URL**
   - **anon public** 키
5. 왼쪽 **Authentication → Users → Add user** 로 **관리자 계정**을 만드세요
   - 이메일·비밀번호를 정해서 넣으시면 됩니다 (이게 앞으로 관리자 로그인 계정입니다)
   - ⚠️ 이걸로 지금 못 들어가고 있는 관리자 비번 문제도 같이 해결됩니다

### ② 카카오 개발자센터 앱 만들기 (10분)

후기 페이지에 회원 로그인을 붙이는 데 필요합니다. **나중에 하셔도 됩니다**
(로그인 없이도 나머지는 다 작동하고, 후기 페이지는 "준비 중"으로 뜹니다).

자세한 순서는 `마케팅의 정석 홈페이지 관리자.md` **14장·15장**에 화면까지
정리돼 있습니다. 핵심만 적으면:
1. https://developers.kakao.com → 애플리케이션 추가
2. **카카오 로그인 켜기** + **OpenID Connect 켜기** (기본이 꺼져 있습니다)
3. Redirect URI에 `https://mail100hanwon.co.kr/auth/kakao/callback` 등록
4. 동의항목에서 **닉네임만** 필수 동의로 설정 (이메일은 넣지 마세요)
5. **REST API 키**와 **Client Secret** 복사해서 주세요

### ③ 저에게 알려주실 값

```
Supabase Project URL :
Supabase anon key    :
관리자 이메일         :
(카카오 REST API 키)  :
(카카오 Client Secret):
```

---

## 그다음은 제가 합니다

1. Vercel에 환경변수 5개 넣기
2. Supabase SQL Editor에서 `supabase/01~05` 순서대로 실행
   (**05번이 제일 중요합니다** — 이걸 안 하면 카카오로 로그인한 아무 환자나
   병원 정보를 고칠 수 있게 됩니다)
3. 관리자 로그인해서 6개 메뉴 전부 눌러보며 확인
4. 칼럼 한 편 시험 삼아 써 보고 사이트에 뜨는지 확인
5. 문제 없으면 `main`에 합치고 배포

---

## 확인해 주셔야 할 결정 1가지

**칼럼은 지금처럼 글 파일(.md)로 두기로 했습니다.** 이유는:
- 이미 39편이 파일로 있고, 그 주소들이 검색엔진에 쌓여 있습니다
- 관리 앱 연결(문 3개)이 파일 방식에 맞춰 만들어져 있습니다
- Supabase로 옮기면 둘 다 다시 만들어야 하는데, 이득이 거의 없습니다

그래서 **칼럼만 파일, 나머지(병원정보·팝업·후기·회원)는 Supabase**입니다.
다르게 원하시면 말씀해 주세요.

---

## 안전장치

- 작업은 전부 `admin-standard` 브랜치 — **라이브 사이트는 지금 그대로입니다**
- 칼럼 39편은 손대지 않았습니다
- Supabase 연결 전에도 빌드·화면이 죽지 않게 만들어 뒀습니다
  (연결 안 된 기능은 "준비 중" 안내가 뜹니다)
