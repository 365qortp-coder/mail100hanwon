# mail100hanwon.co.kr — SEO·GEO 적용 목록

> 작성 2026-08-10 · 검사 도구: `C:\claude\mail100hanwon\_도구\`
> 검사 범위: 배포된 주요 페이지 **17장**을 실제로 내려받아 검사 (칼럼 60여 개는 미검사)
> **아직 아무 파일도 고치지 않았다. 이 문서는 "무엇을 어디서 어떻게 고칠지"의 지시서다.**

---

## 0. 한 장 요약

| 등급 | 건수 | 무엇 |
|---|---|---|
| 치명 | **0** | 없음. 검색에 아예 안 나오는 상태는 아니다 |
| **법위험** | **30** | 상품·판매 스키마 + **FAQ 답변 본문**의 치료경험담·효과 보장 |
| 높음 | 7 | 봇에게 안 보이는 설명표 · H1 없음 3장 · 조직 표 url 없음 |
| 중 | 3 | 제목 계층 · 설명 길이 |

**잘 돼 있는 것** (건드리지 말 것)

- `robots.txt` — 네이버 봇(Yeti)·AI 봇 8종 허용. **검사에서 "문제 없음"**
- `sitemap.xml` — 80여 페이지 등록, robots.txt에 위치 명시
- 설명표 점수 **79~93점** (매우 높음) · 상호·주소·전화 일치
- 지역 페이지 17개 + 역 페이지 4개 — 지역 검색 설계가 제대로 돼 있다

---

## 1. 오늘 — 30분 안에 끝나는 것

### 1-1. 🔴 심의필 문구에 번호가 없다

사이트 하단에 이런 문구가 있다.

> "본 사이트의 의료광고는 대한한의사협회 의료광고 심의를 받은 콘텐츠만 게시합니다."

**그런데 심의번호가 없다.**

| 실제 상황 | 해야 할 것 |
|---|---|
| 심의를 받았다 | **심의필 번호를 함께 표기** |
| 안 받았다 | **오늘 이 문구를 내린다** — 번호 없는 심의 주장은, 실제로 안 받았을 경우 그 문구 자체가 거짓 광고가 된다 |

> 이건 코드 문제가 아니라 **사실 확인** 문제다. 원장님만 답할 수 있다.

### 1-2. 🔴 "재고 있음" · "판매자" 두 줄 삭제

**왜** — 이 두 줄이 붙으면 기계가 읽는 뜻이 이렇게 된다: *"한의원이 한약을 재고로 갖고 있고, 판매자로서 판다."*
같은 페이지의 "비대면 처방 → 전국 택배 발송"과 합쳐지면 **의료법이 아니라 약사법(의약품 판매) 쪽 그림**이 된다.
게다가 처방 후 조제라면 **사실과도 다르다**(거짓 광고).

**어디 — 6개 파일 7묶음**

| 파일 | 줄 |
|---|---|
| `src/lib/schema.ts` | 132, 133 |
| `src/app/diet/page.tsx` | 193, 194 |
| `src/app/gongjindan/page.tsx` | 148, 149 |
| `src/app/gongjindan/sahyang/page.tsx` | 120, 121, 128, 129 |
| `src/app/gongjindan/nokyong/page.tsx` | 82, 83, 90, 91 |
| `src/app/gongjindan/chongmyeong/page.tsx` | 82, 83, 90, 91 |

**무엇을 지우나** — 이 두 줄만. 나머지 형태 교체는 2-3에서.

```ts
availability: "https://schema.org/InStock",     // ← 삭제
seller: { "@id": clinic.url + "#clinic" },      // ← 삭제
// (파일에 따라 seller: { "@type": "Organization", name: clinic.name } 형태)
```

---

## 2. 이번 주 — 코드 수정

### 2-1. 🟠 설명표가 봇에게 안 보인다 (가장 아까운 손해)

**무슨 일인가** — 이 프로젝트는 설명표 넣는 방법이 **두 가지가 섞여 있다.**

| 방법 | 결과 |
|---|---|
| `JsonLd` 컴포넌트 (`src/components/JsonLd.tsx`) — 순수 `<script>` | ✅ **HTML에 들어감** |
| `next/script` 의 `<Script>` | ❌ **자바스크립트로만 주입** → 봇이 못 봄 |

`next/script`는 원래 광고·분석 스크립트를 **나중에 늦게 불러오라고** 만든 도구다. 설명표에 쓰면 서버가 보내는 HTML에 안 들어간다. **AI 봇은 대부분 자바스크립트를 실행하지 않는다.**

**실제로 안 보이고 있는 것** (내려받은 HTML로 확인함)

| 페이지 | 봇이 못 보는 표 |
|---|---|
| `/faq` | **FAQ 표 전체** — 자주 묻는 질문 전용 페이지인데 정작 FAQ 표가 없다 |
| `/treatments/*` 3장 | **진료 표 + FAQ 표** |
| 칼럼 상세 **60여 개** | **글 표(BlogPosting)** |
| 거의 모든 하위 페이지 | 이동경로 표(BreadcrumbList) |

**고칠 파일 5개** — 이미 있는 `JsonLd` 컴포넌트로 갈아끼우면 끝. 새로 만들 것 없다.

| 파일 | 되살아나는 것 |
|---|---|
| `src/app/faq/page.tsx` | FAQ 표 |
| `src/app/treatments/[slug]/page.tsx` | 진료 표 + FAQ 표 |
| `src/app/columns/[slug]/page.tsx` | 글 표 |
| `src/components/ColumnDetailPage.tsx` | 글 표 (칼럼 60여 개) |
| `src/components/Breadcrumb.tsx` | 이동경로 표 (전 페이지) |

**바꾸는 내용**

```tsx
// 지금
import Script from "next/script";
<Script id="faq-schema" type="application/ld+json"
        dangerouslySetInnerHTML={jsonLdScript(faqSchema(...))} />

// 이렇게
import { JsonLd } from "@/components/JsonLd";
<JsonLd id="faq-schema" data={faqSchema(...)} />
```

### 2-2. 🔴 FAQ 답변 본문 — 치료경험담·효과 보장

**이번 검사에서 가장 무거운 항목이다.**
표의 **종류**는 안전한데(FAQPage) 그 안의 **문장**이 위반이다. 그리고 FAQ 답변은 **AI가 가장 잘 집어가는 칸**이라 화면 카피보다 위험하다.

| 파일:줄 | 문제 문장 | 무엇에 걸리나 |
|---|---|---|
| `diet/page.tsx:46` | "실제 후기에서 … **8.7kg 감량**, … 82일 4.6kg 감량한 **사례가 있습니다**" | 치료경험담 |
| `diet/page.tsx:58` | "**가장 빠른 사례는 10일 만에 2.5kg 감량**입니다 … 평균은 48일 4.5kg" | 치료경험담 + 최상급 |
| `diet/page.tsx:38` | "… 올바른 단계로 복용하면 **부작용 없이** … **가능합니다**" | 효과·안전성 보장 |
| `diet/page.tsx:49-50` | "**마운자로·위고비** 대신 감비환을 선택한 사례가 다수" | 전문의약품 실명 거론 (약사법 별도) |
| `gongjindan/sahyang/page.tsx:37` | "**식약처 인증** 정품 사향만 사용하며" | 인증 표방 — 그런 제도가 실제 있는지 확인 필요 |
| `data/treatments.ts:109-110` | "**재처방률 72%**. 효과를 경험한 환자가 반복 선택한 비율" | 성공률을 효과의 증거로 |
| `diet/page.tsx:13, 187` | "2026년 7만건 이상 처방, **오프라인 내원 89.9% 체지방 위주 감량**. 2~3달 감량 + 6개월 요요방지" | 치료 성공률 + 결과 보장 |

> **왜 자동 필터로 못 잡나** — "완치·보장" 같은 금칙어가 아니라 **문장 구조**로 위반한다. "…한 사례가 있습니다"에는 금칙어가 하나도 없다.

**대체 방향** (의료광고 판단·문장 재작성 모두 대화로 — 기준은 `.claude\의료광고-법조문-기록.md`)

| 지금 | 이렇게 |
|---|---|
| "…한 사례가 있습니다" | 사례를 빼고 **절차**로 — "감량기 2~3개월, 유지 관리기 6개월로 나누어 복용 계획을 세웁니다" |
| "부작용 없이 가능합니다" | 한계를 밝힌다 — "마황이 포함되므로 1~8단계로 복용량을 조정합니다" |
| "재처방률 72%" | 삭제. 대체하지 않는다 |
| "식약처 인증" | 제도 확인 전에는 쓰지 않는다 |
| 전문의약품 실명 비교 | 다른 약을 언급하지 않는다 |

### 2-3. 🟠 상품 스키마를 진료 절차 스키마로 교체

1-2에서 두 줄만 급히 뺐다면, 이제 형태 자체를 바꾼다.

**왜 GEO 손해가 거의 없나** — AI는 스키마 **종류**보다 **문장 내용**을 보고 인용한다. 오히려 절차·시간·횟수 서술이 인용률이 높다.

**대체 스키마 — /diet**

```ts
{
  "@context": "https://schema.org",
  "@type": "MedicalProcedure",
  "@id": clinic.url + "/diet#procedure",
  name: "매일감비환 한약 처방",
  description: "40대~갱년기 여성의 기초대사 저하에 초점을 둔 한약 처방입니다. 상담에서 체질과 생활 패턴을 확인한 뒤 처방 방향을 정합니다.",
  procedureType: "https://schema.org/NoninvasiveProcedure",
  howPerformed: "전화 또는 카카오톡 문의 → 설문지 작성 → 대표원장 상담 → 처방. 감량기 2~3개월, 유지 관리기 6개월로 나누어 복용 계획을 세웁니다. 복용량은 1~8단계로 조정합니다.",
  preparation: "복용 중인 약, 기저질환, 수면·식사 패턴을 상담 시 확인합니다.",
  followup: "복용 반응에 따라 단계를 조정하며 경과를 확인합니다.",
  provider: { "@id": clinic.url + "#clinic" }
}
```

**대체 스키마 — /gongjindan**

```ts
{
  "@context": "https://schema.org",
  "@type": "MedicalProcedure",
  "@id": clinic.url + "/gongjindan#procedure",
  name: "공진단 처방",
  description: "「동의보감」 공진단 처방 구성에 따라 원내에서 조제합니다. 사향공진단, 녹용 2배 공진단, 총명공진단 세 가지 구성이 있습니다.",
  procedureType: "https://schema.org/NoninvasiveProcedure",
  howPerformed: "문의 → 설문지 작성 → 대표원장 상담 → 복용 목적과 체질에 따라 구성 결정 → 원내 조제.",
  preparation: "복용 목적, 기저질환, 복용 중인 약을 상담에서 확인합니다.",
  provider: { "@id": clinic.url + "#clinic" }
}
```

**뺀 칸과 이유**

| 뺀 것 | 왜 |
|---|---|
| `Product`, `brand` | 한약은 상품·브랜드가 아니라 처방 |
| `offers`, `price`, `priceCurrency` | 가격은 §3-1의 별도 페이지로 |
| `availability` | 재고 개념 자체가 성립 안 함 |
| `seller` | 판매자가 아니라 `provider`(제공자) |
| 7만건 · 89.9% · 2~3달 감량 | 효과 수치·결과 단정 |

### 2-4. 🟠 큰제목(H1)이 없는 페이지 3장

`clinic` · `columns` · `faq` 세 장에 **H1이 하나도 없다.** 이 페이지의 주제를 기계가 잡지 못한다.

**원인** — `src/components/Section.tsx`의 `SectionTitle`이 제목을 항상 `<h2>`로 만든다. 이걸로만 제목을 세운 페이지는 H1이 없다.

**고치는 법** — `SectionTitle`에 "H1으로 쓸지" 고르는 옵션을 붙이고, 세 페이지에서 켠다.

```tsx
export function SectionTitle({ eyebrow, title, subtitle, align = "left", as = "h2" }: {
  /* … */ as?: "h1" | "h2";
}) {
  const H = as;
  /* … */
  <H className="font-serif text-3xl md:text-4xl …">{title}</H>
}
```

### 2-5. 🟡 자잘한 것

| 무엇 | 어디 | 조치 |
|---|---|---|
| 조직 표에 `url` 없음 | `/diet`, `/gongjindan` | `url: clinic.url` 한 줄 추가 |
| 설명이 177자로 길어 잘림 | `/treatments/gongjindan` | 120자 안쪽으로 |
| 소제목이 H2 다음 H4 | `/about` | H3로 |

---

## 3. 그다음

### 3-1. "비급여 진료비용 안내" 페이지 신설

**이건 규제 회피가 아니라 의무 이행이다.** 그래서 오히려 준법 증거가 된다.

- 감비환·공진단 **전 구성을 하나도 빠짐없이** 같은 형식의 표로
- "부터", "시작가", "최저" 같은 표현 쓰지 않음
  (지금 스키마는 가장 싼 110,000원 하나만 기계에 넘기고 있다 → "불명확한 가격 정보"로 지적받을 여지)
- 할인·이벤트 표기 없음

### 3-2. 칼럼 60여 개 전체 검사

이번엔 주요 17장만 봤다. 칼럼까지 돌리면 "글 표 없음"이 60건대로 늘어난다(2-1을 고치면 한 번에 해결).

---

## 4. 내 담당 밖 — 반드시 따로 처리할 것

### 4-1. 화면 카피에도 같은 문제가 있다

스키마만 고치면 절반이다. **화면에 보이는 숫자 카드에도** 같은 문구가 있다.

| 파일:줄 | 화면에 보이는 것 |
|---|---|
| `diet/page.tsx:220, 324` | **89.9%** "체지방 위주 감량" |
| `gongjindan/page.tsx:188, 214` | **72%** "재처방률" |
| `gongjindan/nokyong/page.tsx:125, 253` | 재처방률 72% — "효과를 체감한 환자들의 재선택" |
| `gongjindan/sahyang/page.tsx:160, 338` | 재처방률 72% · "식약처 인증 정품 사향" |
| `diet/page.tsx:482~514` | 마운자로·위고비 **vs** 매일감비환 비교표 |

→ **의료광고 검수 + 문장 재작성**이 필요하다. SEO 담당이 카피를 고치지 않는다.

### 4-2. 협회에 물어야 할 것

1. **화면에 안 보이는 설명표(JSON-LD)도 심의 제출물에 포함해야 하는가** — 대행 상품 설계에 직결
2. "식약처 인증 정품 사향"의 인증 제도가 실제로 있는지
3. **비대면 진료 + 한약 택배 발송**의 적법 범위 — 약사법 영역

---

## 5. 다 고친 뒤 검증 — 이 순서로

```bash
npm run build
```

```bash
python C:\claude\mail100hanwon\_도구\seocheck.py <내려받은폴더> --type a
```

**통과 기준**

| 항목 | 목표 |
|---|---|
| 치명 | 0 (지금도 0) |
| **법위험** | **0** ← 이번 작업의 핵심 |
| `/faq` 의 FAQPage가 **HTML 안에** | 있음 |
| 칼럼 상세의 BlogPosting이 **HTML 안에** | 있음 |
| H1 없는 페이지 | 0장 |

**눈으로 확인하는 법** — 브라우저 주소창에 `view-source:https://mail100hanwon.co.kr/faq` 를 치고 `FAQPage` 를 찾는다.
`<script type="application/ld+json">` **안에** 있어야 한다. `self.__next_f.push` 안에 있으면 아직 안 고쳐진 것이다.

---

## 6. 안 하는 것 / 못 하는 것

- **배포하지 않는다.** 수정·검증까지가 SEO 담당의 몫이다
- **카피 문장을 직접 고치지 않는다** → 별도 작업으로 뺀다
- **의료법 최종 판정을 하지 않는다** → 협회 심의기관
- **실제 색인 여부·페이지 속도는 이 도구로 못 본다** → 구글 서치콘솔, PageSpeed Insights
- **순위·AI 인용은 보장할 수 없다.** 보장하는 건 "읽을 수 있는 상태로 만든 것"까지다

---

## 7. 실행 체크표 (복사해서 쓴다)

```
오늘
[ ] 심의필 문구 — 심의 실제로 받았는지 확인 → 번호 표기 or 문구 삭제
[ ] availability(재고) 삭제 — 6개 파일
[ ] seller(판매자) 삭제 — 6개 파일

이번 주
[ ] next/script → JsonLd 교체 (5개 파일)
[ ] FAQ 답변 본문 재작성 (의료광고 검수 동반)
[ ] Product → MedicalProcedure 교체 (/diet, /gongjindan)
[ ] SectionTitle에 as="h1" 옵션 + 3개 페이지 적용
[ ] 조직 표 url 추가 · description 길이 조정 · H4→H3

그다음
[ ] 비급여 진료비용 안내 페이지
[ ] 칼럼 전체 검사
[ ] 화면 카피 검수 (89.9% · 재처방률 72% · 식약처 인증 · 비교표)
[ ] 협회 질의 3건

검증
[ ] npm run build 성공
[ ] seocheck 법위험 0
[ ] view-source로 /faq 의 FAQPage 확인
```
