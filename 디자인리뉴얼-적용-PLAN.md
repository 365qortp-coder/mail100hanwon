# 디자인리뉴얼 → 실사이트(Next.js) 적용 기획

2026-08-05 확정. 디자인 기준은 [DESIGN-BASELINE.md](DESIGN-BASELINE.md) 참조.
원칙: **콘텐츠는 실사이트 현행 유지, 디자인만 리뉴얼 기준으로 교체.**

## 1단계 — 공통 요소 (홈·서브 전체에 깔리는 것)

| 대상 | 현재 (src) | 변경 (리뉴얼 기준) |
|---|---|---|
| `globals.css` | 크림 배경 변수, 베젤 카드 관행 | 화이트 베이스 + 아이보리 포인트 팔레트, 카드 호버(lift+줌), grain 오버레이 추가 |
| `Header.tsx` | 현행 내비 | h-14 sticky + 이미지 로고 h-9 + 카톡/비대면 버튼 + 모바일 가로스크롤 pill 내비 |
| `Footer.tsx` | 현행 | 리뉴얼 FOOTER(다크) 구조로 |
| `FloatingCTA.tsx` | 현행 | 리뉴얼 모바일 하단 고정 CTA바(전화·카톡·비대면)로 |
| 아이콘 | 없음/텍스트 화살표 | solar 아이콘 모양의 인라인 SVG 컴포넌트 (외부 CDN 스크립트 금지) |

## 2단계 — 홈(`src/app/page.tsx`) 섹션별

| 섹션 | 작업 |
|---|---|
| 01 HERO | 배경 크림→화이트. 원장 사진에 **플로팅 이름배지**(송원석·대전대 한의대·2016 개원) + **"비대면 전국 처방" 배지** 추가. CTA는 현행 유지(전화 0507-1467-0195) |
| 02 STATS | 숫자 `text-5xl md:text-6xl`로 확대, eyebrow `white/35`로 |
| 03 PRODUCTS | 베젤 프레임 제거 → 보더 카드 + 호버 lift + 이미지 줌. 공진단 카드 다크 변형 유지 |
| 03B 대사량 훅 | **신규 섹션** — 다크 + 레드 radial glow, "기초대사량이 떨어졌다면?" 카피, 증상 칩 3개, CTA 2개(매일감비환 / 계산기). 리뉴얼 601~638행 이식 |
| 04 ABOUT | 유튜브와 **순서 교체**(About이 먼저). 배경 화이트, 정보 그리드 `#F5F5F5` 카드로 |
| 05 YOUTUBE | 순서만 About 뒤로, 스타일 리뉴얼 톤 맞춤 |
| 06 PERSONAS | **다크 → 아이보리(#F8F6F2)** 카드 그리드로 교체 (리뉴얼 858행~) |
| 07 COLUMNS | 베젤 제거 → col-card 스타일 (보더+호버 lift). 데이터는 현행 최신 칼럼 그대로 |
| 08 LOCATIONS | `#F5F2EC` 배경 + loc-pill 호버 스타일 |
| 09 FAQ | 화이트 배경 아코디언, faq-item 보더 호버 |
| 10 FINAL CTA | 리뉴얼 다크 대형 헤딩 스타일 미세 조정 |

## 3단계 — 기초대사량 계산기 이식

- `디자인리뉴얼\기초대사량-계산기.html` → Next.js 라우트 신설 (라우트명 제안: `/bmr` — 한글 URL은 색인·공유에서 깨짐)
- 훅 섹션·감비환 페이지에서 링크, sitemap 등록
- 계산 로직은 정적 JS 그대로 클라이언트 컴포넌트로

## 4단계 — 배포·검증

- git commit → push → Vercel 자동빌드 (**vercel CLI 금지** — 7/2 덮어쓰기 사고 이력)
- 배포 후 mail100hanwon.co.kr 모바일/데스크탑 스크린샷으로 확인
- 홈 확인 받은 뒤 서브 진행

## 5단계 — 서브페이지 순차 적용

순서: `diet` → `gongjindan`(+사향·녹용·총명 하위) → `nmc` → `about` → `columns`(목록·상세) → 템플릿 페이지(locations/stations/treatments)
각각 리뉴얼의 대응 HTML(diet.html 등)과 대조해 같은 방식으로.

## 리스크 (미리 알아둘 것)

- **디자인리뉴얼 폴더의 `.vercel`이 실사이트 프로젝트를 가리키고 있었음** → 사고 방지 위해 링크 제거함 (다시 정적 배포하려면 `vercel link`로 별도 프로젝트 재연결 필요)
- 리뉴얼 HTML의 전화번호(02-2234-0102)·네이버 예약 단축링크는 낡은 값 — 이식 시 실사이트 `data/clinic.ts` 값으로 대체할 것
- 홈 섹션 순서 변경(About↔YouTube)은 앵커 링크가 있다면 확인 필요
- PERSONAS 다크→아이보리 전환으로 홈의 다크/라이트 리듬이 바뀜: 다크는 STATS·훅·FINAL CTA 3곳이 됨 (리뉴얼과 동일)
